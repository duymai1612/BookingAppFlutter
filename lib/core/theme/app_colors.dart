import 'package:flutter/material.dart';

/// Central colour palette derived from Booking Link design mockup.
class AppColors {
  AppColors._();

  static const lightBackground = Color(0xFFF5F9F6); // subtle minty light grey
  static const border = Color(0xFFE3EAE6);
  static const black = Color(0xFF000000);
  static const green =
      Color(0xFF27AE60); // primary accent for success/highlight
  static const bulletGrey = Color(0xFFD1D8D4);
}
